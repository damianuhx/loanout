<h3>Reinventur (nur Admin)</h3>

<p>Als Admin (Lagerverwalter) kann nur der Bestand verwaltet werden. Deshalb
werden unter "Objektauswahl" unvollständige, fehlende und defekte Objekte angezeigt. Diese können "ausgeliehen" werden
um den Status dieser Objekte zu erneuern. Die Kundenadresse muss hier zwar angegeben werden, hat aber keinen Einfluss. Wird ein Objekt ausgeliehen so ändert der Status direkt zu "versendet" und das
Objekt kann "zurückgenommen" werden. Dabei kann die Funktionstüchtigkeit und das vorhandene Zubehör neu angegeben werden. </p>
